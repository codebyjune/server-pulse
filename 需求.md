# 服务器状态监控看板 项目需求文档（PRD）

**版本**：v1.0  
**日期**：2026年3月  
**项目负责人**：你（个人开发）  
**目标用户**：你自己 + 简历展示（可扩展给小团队使用）  
**核心定位**：利用你现有的 VPS 实现一个真实生产级的服务器实时监控看板，数据全部来自你这台 VPS（可后续扩展多台服务器），用于个人项目实战 + 简历亮点。

---

## 1. 项目背景与目标

你已经拥有一台 VPS（假设为 Linux 系统，如 Ubuntu/Debian/CentOS），希望将其打造成一个完整、可演示、能上线的监控系统。

**业务目标**：
- 实时监控 VPS 的核心指标（CPU、内存、磁盘、网络、负载、进程等）
- 提供美观、响应式的可视化看板（支持 PC + 平板）
- 支持秒级实时更新 + 历史数据查询 + 告警
- 项目完成后可部署在你自己的 VPS 上（前端 + 后端全栈），GitHub + 在线 Demo 直接放简历
- 最终效果：打开浏览器就能看到你这台 VPS 正在“活生生”地跳动数据，面试官一眼就能看出是真实项目

**成功标准**：
- 实时延迟 ≤ 2 秒
- 单页支持 60 FPS 流畅渲染
- 支持至少 30 天历史数据查询
- 可通过浏览器直接访问（`https://你的域名/monitor`）

---

## 2. 用户角色

- **管理员**（你自己）：查看所有数据、设置告警阈值、管理服务器列表
- **访客**（面试官/他人）：只读模式查看公开仪表盘（可选）

---

## 3. 功能需求（优先级：Must / Should / Could）

### 3.1 仪表盘首页（Must）
- **概览卡片**：CPU 使用率、内存使用率、磁盘使用率、当前负载（1/5/15min）、网络上下行实时速率
- **大屏图表**：
  - CPU/内存/磁盘折线图（过去 1h / 6h / 24h 可切换）
  - 实时仪表盘（Gauge）+ 进度条
  - 网络 I/O 折线图 + 当前带宽
- **服务器基本信息**：主机名、IP、系统版本、运行时长（uptime）、在线状态

### 3.2 实时监控模块（Must）
- WebSocket 长连接推送（或 SSE）
- 每 1～3 秒更新一次全部指标
- 支持手动刷新 + 暂停/恢复
- 多服务器切换（当前只接入你这台 VPS，后续可添加）

### 3.3 历史数据与图表（Must）
- 时间范围选择：最近 1h、6h、24h、7天、30天
- 图表类型：折线图、面积图、柱状图
- 支持导出 CSV（可选）
- 数据存储在 VPS 本地 SQLite / MongoDB / InfluxDB（推荐）

### 3.4 告警系统（Should）
- 阈值设置：CPU > 85%、内存 > 90%、磁盘 > 85% 等
- 告警方式：浏览器通知 + 邮件（推荐使用 Nodemailer + 163/QQ邮箱）
- 告警历史记录列表（最近 50 条）
- 告警静音/解除

### 3.5 系统管理（Should）
- 服务器列表管理（当前只有 1 台，可扩展）
- 主题切换（浅色/深色）
- 用户登录（简单 JWT 或直接本地模式）
- 设置页面：WebSocket 端口、刷新间隔、告警邮箱

### 3.6 其他（Could）
- 进程列表（top 类似，点击可杀进程）
- Docker 容器监控（如果你的 VPS 跑 Docker）
- 温度/风扇（如果服务器支持）
- 移动端适配 + PWA

---

## 4. 非功能需求

- **性能**：单页同时监控 1 台服务器时，CPU 占用 < 5%，内存 < 200MB
- **实时性**：WebSocket 心跳 + 重连机制，断线后 3 秒内自动重连
- **安全性**：HTTPS（用 Nginx + Let's Encrypt）、API Token 验证、防止直接暴露端口
- **兼容性**：Chrome、Edge、Safari 最新版 + 响应式布局（手机也能看）
- **数据保留**：至少保存 30 天历史数据（可配置）
- **部署要求**：全部跑在你这台 VPS 上（一个 Docker Compose 启动全部服务）

---

## 5. 数据来源与采集方案（针对你的 VPS）

推荐方案（由易到难，任选其一）：

**方案 A**（推荐，最简单真实）
- 使用 `systeminformation`（Node.js）或 `node-os-utils` 直接在后端采集
- 或安装 `prometheus-node-exporter`（标准方式）+ 后端用 Prometheus API 读取

**方案 B**（更专业）
- 安装 `node-exporter` + Prometheus + Grafana（但你自己写前端，不用 Grafana UI）

**方案 C**（极简）
- 每秒执行 shell 命令（top、free、df、netstat 等）解析输出

**数据采集频率**：实时模块 1～3 秒，历史数据每 10 秒存一次。

---

## 6. 推荐技术栈（2026 年主流 + 简历加分）

**前端（推荐）**：
- React 18 + TypeScript + Vite
- UI：TailwindCSS + shadcn/ui 或 Ant Design
- 图表：ECharts（最强大）或 Recharts
- 状态管理：Zustand / Jotai
- WebSocket：原生 WebSocket + reconnecting-websocket

**后端**：
- Node.js + Express / Fastify（或 NestJS）
- 数据库：SQLite（轻量）或 MongoDB / InfluxDB（时序）
- WebSocket：`ws` 库 或 Socket.io

**部署（全部在你的 VPS）**：
- Docker Compose（推荐）
  - 容器1：后端 + WebSocket
  - 容器2：Nginx（反向代理 + HTTPS）
  - 容器3：数据库
- 域名 + Cloudflare / 阿里云解析（免费）
- PM2 或 Docker 保活

**备选**（如果你更熟悉）：
- Vue3 + Pinia + Element Plus
- Python FastAPI + FastAPI-WebSocket

---

## 7. 部署与上线方案（你的 VPS 专属）

1. VPS 开放端口：80、443（Nginx）、3000（后端可选）
2. 使用 Docker Compose 一键启动：

```yaml
version: '3.8'
services:
  backend:
    build: ./backend
    ports: ["3001:3001"]
  frontend:
    build: ./frontend
    depends_on: [backend]
  nginx:
    image: nginx:alpine
    ports: ["80:80", "443:443"]
    volumes: [./nginx.conf:/etc/nginx/conf.d/default.conf]