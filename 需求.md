# Server Pulse - 服务器状态监控看板

**版本**：v2.0
**日期**：2026年3月
**状态**：开发中
**目标**：个人项目实战 + 简历亮点 + 面试作品

---

## 项目进度

| 模块 | 状态 | 说明 |
|-----|------|------|
| 仪表盘首页 | ✅ 完成 | 概览卡片 + 趋势图 + 系统信息 |
| 实时监控 | ✅ 完成 | WebSocket 每2秒推送 |
| 历史数据 | 🚧 进行中 | 需要数据库存储 |
| 用户认证 | 📋 待开发 | JWT + 多用户支持 ⭐ |
| 多服务器监控 | 📋 待开发 | Agent 模式 ⭐ |
| 告警系统 | 📋 待开发 | 阈值告警 + 多渠道通知 |
| 数据统计 | 📋 待开发 | 报表 + 分析 ⭐ |
| 系统设置 | 📋 待开发 | 主题切换 + 配置管理 |
| 进程管理 | 📋 待开发 | 进程查看 + 操作 ⭐ |
| 网络监控 | 📋 待开发 | 接口 + 连接状态 ⭐ |
| Docker部署 | 📋 待开发 | 一键部署 |

---

## 1. 已完成功能

### 1.1 仪表盘首页 ✅

- **概览卡片**
  - CPU 使用率（带进度条）
  - 内存使用率（带进度条）
  - 磁盘使用率（带进度条）
  - 网络速率（KB/s）

- **系统信息卡片**
  - 主机名
  - 操作系统
  - CPU 核心数
  - 运行时长（天/小时/分）

- **趋势图**
  - CPU/内存折线图
  - 实时滚动更新（保留30个数据点）
  - 支持 tooltip 悬停查看详情

### 1.2 实时监控 ✅

- WebSocket 长连接（Socket.io）
- 每 2 秒推送一次数据
- 断线自动重连（React 重新挂载时重连）

### 1.3 导航路由 ✅

- 侧边栏菜单（仪表盘/实时/历史/告警/设置）
- React Router 路由管理
- 高亮当前页面

---

## 2. 待完成功能

### 2.1 历史数据模块（Must）

**功能描述**：
- 时间范围选择：1h / 6h / 24h / 7天 / 30天
- 历史趋势图展示
- 数据导出 CSV（可选）
- 数据聚合统计（平均值、最大值、最小值）

**技术方案**：
- 数据库：SQLite（轻量，单文件）
- 存储：每 10 秒存一条记录
- 查询：按时间范围分页查询
- 聚合：大数据量时按小时/天聚合

**接口设计**：
```
GET /metrics/history?range=1h&limit=100
GET /metrics/stats?range=24h  # 聚合统计
```

### 2.2 告警系统（Should）

**功能描述**：
- 阈值设置：CPU > 85%、内存 > 90%、磁盘 > 85%
- 浏览器通知（Notification API）
- 告警历史列表（最近 50 条）
- 告警静音/解除
- 告警级别：警告 / 严重 / 紧急
- 告警恢复通知
- 邮件通知（可选）

**技术方案**：
- 前端：Notification API
- 后端：阈值检测 + 事件记录
- 存储：告警记录存数据库
- 邮件：NestJS Mailer 模块

**接口设计**：
```
GET  /alerts              # 获取告警列表
GET  /alerts/unread-count # 未读告警数
POST /alerts/threshold    # 设置阈值
POST /alerts/read         # 标记已读
POST /alerts/mute         # 静音/解除
```

### 2.3 系统设置（Should）

**功能描述**：
- 主题切换（浅色/深色）
- 刷新间隔设置（1s/2s/5s）
- 告警阈值配置
- 数据保留天数
- 语言切换（中文/英文）
- 通知偏好设置

**技术方案**：
- 主题：Semi-UI Theme API
- 配置：localStorage + 后端同步
- 国际化：react-i18next

### 2.4 Docker 部署（Must）

**功能描述**：
- Docker Compose 一键启动
- Nginx 反向代理
- 生产环境配置
- 健康检查
- 自动重启策略

**文件结构**：
```
docker-compose.yml
Dockerfile (前端)
Dockerfile (后端)
nginx.conf
.env.example
```

### 2.5 用户认证系统（Must）⭐ 新增

**功能描述**：
- 用户注册 / 登录 / 登出
- JWT Token 认证
- 记住登录状态
- 修改密码
- 登录日志记录

**技术方案**：
- 认证：JWT + Passport
- 密码：bcrypt 加密
- 存储：用户表存 SQLite
- 安全：Rate Limiting、CORS

**接口设计**：
```
POST /auth/register     # 注册
POST /auth/login        # 登录
POST /auth/logout       # 登出
GET  /auth/profile      # 获取当前用户
PUT  /auth/password     # 修改密码
```

### 2.6 多服务器监控（Agent 模式）（Must）⭐ 新增

**功能描述**：
- 添加/删除/编辑监控服务器
- 服务器分组管理
- 单台服务器详情页
- 多服务器概览对比
- 服务器状态标签（在线/离线/异常）
- Agent 心跳检测

**技术方案**：
- 架构：中心服务器 + Agent 客户端
- 通信：Agent 主动连接中心服务器（WebSocket / HTTP 轮询）
- Agent：独立 CLI 工具，跨平台支持
- 发现：支持自动发现局域网服务器

**Agent 设计**：
```
server-pulse-agent/
├── src/
│   ├── index.ts           # 入口
│   ├── collector.ts       # 指标采集
│   ├── reporter.ts        # 数据上报
│   └── config.ts          # 配置管理
└── package.json
```

**接口设计**：
```
# 服务器管理
GET    /servers           # 获取服务器列表
POST   /servers           # 添加服务器
PUT    /servers/:id       # 编辑服务器
DELETE /servers/:id       # 删除服务器
GET    /servers/:id/metrics # 单台服务器指标

# Agent 通信
POST   /agent/register    # Agent 注册
POST   /agent/heartbeat   # Agent 心跳
POST   /agent/metrics     # Agent 上报指标
```

### 2.7 数据统计与分析（Should）⭐ 新增

**功能描述**：
- 资源使用趋势分析
- 峰值时段统计
- 异常时段标记
- 日报 / 周报 / 月报
- 导出 PDF 报告

**技术方案**：
- 统计：后端定时任务聚合
- 报告：PDF 生成（puppeteer / pdfkit）
- 存储：统计数据单独存表

**接口设计**：
```
GET /analytics/summary   # 总览统计
GET /analytics/peak      # 峰值分析
GET /analytics/report    # 生成报告
```

### 2.8 进程管理（Nice to have）⭐ 新增

**功能描述**：
- 查看系统进程列表
- 进程 CPU/内存占用排序
- 搜索进程
- 结束进程（需权限确认）

**技术方案**：
- 采集：systeminformation.processes()
- 实时：WebSocket 推送进程变化

**接口设计**：
```
GET  /processes          # 进程列表
GET  /processes/:pid     # 进程详情
POST /processes/:pid/kill # 结束进程
```

### 2.9 网络监控（Nice to have）⭐ 新增

**功能描述**：
- 网络接口流量统计
- 端口监听状态
- TCP/UDP 连接数
- 简单的 Ping 检测

**技术方案**：
- 采集：systeminformation.networkInterfaces()
- 端口：systeminformation.networkConnections()

**接口设计**：
```
GET /network/interfaces   # 网络接口
GET /network/connections  # 连接状态
GET /network/ping?host=x  # Ping 检测
```

---

## 3. 技术栈

### 前端
| 技术 | 版本 | 用途 |
|-----|------|------|
| React | 18 | 框架 |
| TypeScript | 5.x | 类型安全 |
| Vite | 8.x | 构建工具 |
| Semi-UI | 2.x | 组件库 |
| ECharts | 5.x | 图表 |
| Socket.io-client | 7.x | WebSocket |
| React Router | 7.x | 路由 |
| TailwindCSS | 4.x | 样式 |

### 后端
| 技术 | 版本 | 用途 |
|-----|------|------|
| NestJS | 11.x | 框架 |
| Socket.io | 4.x | WebSocket |
| systeminformation | 5.x | 系统指标采集 |
| TypeORM | 0.3.x | ORM 框架 ⭐ |
| SQLite | 3.x | 数据存储 |
| Passport | - | 认证中间件 ⭐ |
| JWT | - | Token 认证 ⭐ |
| bcrypt | - | 密码加密 ⭐ |
| class-validator | - | 参数校验 |

### 部署
| 技术 | 用途 |
|-----|------|
| Docker | 容器化 |
| Docker Compose | 编排 |
| Nginx | 反向代理 |

---

## 4. 项目结构

```
server-pulse/
├── src/                      # 前端代码
│   ├── components/
│   │   ├── dashboard/
│   │   │   ├── MetricCard.tsx    # 指标卡片
│   │   │   └── MetricsChart.tsx  # 趋势图表
│   │   ├── layout/
│   │   │   ├── AppLayout.tsx     # 布局
│   │   │   └── Sidebar.tsx       # 侧边栏
│   │   └── common/               # 通用组件 ⭐
│   │       ├── Loading.tsx
│   │       ├── Empty.tsx
│   │       └── ErrorBoundary.tsx
│   ├── hooks/
│   │   ├── useMetrics.ts         # 实时数据
│   │   ├── useMetricsHistory.ts  # 历史数据
│   │   ├── useSystemInfo.ts      # 系统信息
│   │   ├── useAuth.ts            # 认证 ⭐
│   │   └── useServers.ts         # 服务器管理 ⭐
│   ├── pages/
│   │   ├── Dashboard.tsx         # 仪表盘
│   │   ├── Realtime.tsx          # 实时监控
│   │   ├── History.tsx           # 历史数据
│   │   ├── Alerts.tsx            # 告警
│   │   ├── Settings.tsx          # 设置
│   │   ├── Login.tsx             # 登录 ⭐
│   │   ├── Servers.tsx           # 服务器管理 ⭐
│   │   ├── ServerDetail.tsx      # 服务器详情 ⭐
│   │   ├── Processes.tsx         # 进程管理 ⭐
│   │   └── Network.tsx           # 网络监控 ⭐
│   ├── stores/                   # 状态管理 ⭐
│   │   ├── authStore.ts
│   │   └── settingsStore.ts
│   ├── services/                 # API 服务 ⭐
│   │   ├── api.ts
│   │   └── websocket.ts
│   └── App.tsx
│
├── server/                   # 后端代码
│   └── src/
│       ├── modules/
│       │   ├── metrics/
│       │   │   ├── metrics.module.ts
│       │   │   ├── metrics.service.ts
│       │   │   ├── metrics.controller.ts
│       │   │   ├── metrics.gateway.ts
│       │   │   └── entities/
│       │   │       └── metric.entity.ts    ⭐
│       │   ├── auth/                        ⭐
│       │   │   ├── auth.module.ts
│       │   │   ├── auth.service.ts
│       │   │   ├── auth.controller.ts
│       │   │   ├── jwt.strategy.ts
│       │   │   └── entities/
│       │   │       └── user.entity.ts
│       │   ├── servers/                     ⭐
│       │   │   ├── servers.module.ts
│       │   │   ├── servers.service.ts
│       │   │   ├── servers.controller.ts
│       │   │   └── entities/
│       │   │       └── server.entity.ts
│       │   ├── alerts/                      ⭐
│       │   │   ├── alerts.module.ts
│       │   │   ├── alerts.service.ts
│       │   │   ├── alerts.controller.ts
│       │   │   └── entities/
│       │   │       └── alert.entity.ts
│       │   ├── processes/                   ⭐
│       │   │   ├── processes.module.ts
│       │   │   ├── processes.service.ts
│       │   │   └── processes.controller.ts
│       │   └── network/                     ⭐
│       │       ├── network.module.ts
│       │       ├── network.service.ts
│       │       └── network.controller.ts
│       ├── common/                          ⭐
│       │   ├── guards/
│       │   │   └── jwt-auth.guard.ts
│       │   ├── decorators/
│       │   │   └── current-user.decorator.ts
│       │   └── filters/
│       │       └── http-exception.filter.ts
│       ├── database/                        ⭐
│       │   ├── database.module.ts
│       │   └── migrations/
│       ├── app.module.ts
│       └── main.ts
│
├── agent/                    # Agent 客户端 ⭐
│   ├── src/
│   │   ├── index.ts              # 入口
│   │   ├── collector.ts          # 指标采集
│   │   ├── reporter.ts           # 数据上报
│   │   ├── heartbeat.ts          # 心跳检测
│   │   └── config.ts             # 配置管理
│   ├── package.json
│   └── tsconfig.json
│
├── docker-compose.yml        # Docker 编排
├── Dockerfile.frontend       # 前端镜像
├── Dockerfile.backend        # 后端镜像
├── Dockerfile.agent          # Agent 镜像 ⭐
├── nginx.conf                # Nginx 配置
├── .env.example              # 环境变量示例 ⭐
└── README.md
```

---

## 5. API 文档

### 已实现

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/metrics/system` | 获取系统信息 |
| WS | `metrics` | 实时指标推送（每2秒） |

### 认证相关 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/auth/register` | 用户注册 |
| POST | `/auth/login` | 用户登录 |
| POST | `/auth/logout` | 用户登出 |
| GET | `/auth/profile` | 获取当前用户信息 |
| PUT | `/auth/password` | 修改密码 |

### 历史数据

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/metrics/history` | 获取历史数据 |
| GET | `/metrics/stats` | 聚合统计 |

### 服务器管理 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/servers` | 获取服务器列表 |
| POST | `/servers` | 添加服务器 |
| PUT | `/servers/:id` | 编辑服务器 |
| DELETE | `/servers/:id` | 删除服务器 |
| GET | `/servers/:id/metrics` | 单台服务器指标 |

### Agent 通信 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | `/agent/register` | Agent 注册 |
| POST | `/agent/heartbeat` | Agent 心跳 |
| POST | `/agent/metrics` | Agent 上报指标 |

### 告警系统

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/alerts` | 获取告警列表 |
| GET | `/alerts/unread-count` | 未读告警数 |
| POST | `/alerts/threshold` | 设置告警阈值 |
| POST | `/alerts/read` | 标记已读 |
| POST | `/alerts/mute` | 静音/解除 |

### 进程管理 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/processes` | 进程列表 |
| GET | `/processes/:pid` | 进程详情 |
| POST | `/processes/:pid/kill` | 结束进程 |

### 网络监控 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/network/interfaces` | 网络接口 |
| GET | `/network/connections` | 连接状态 |
| GET | `/network/ping` | Ping 检测 |

### 数据分析 ⭐

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | `/analytics/summary` | 总览统计 |
| GET | `/analytics/peak` | 峰值分析 |
| GET | `/analytics/report` | 生成报告 |

---

## 6. 开发计划

### Phase 1（当前）- 核心功能 ✅
- [x] 项目初始化
- [x] 前端布局 + 路由
- [x] 概览卡片组件
- [x] WebSocket 实时推送
- [x] 趋势图组件
- [x] 系统信息展示

### Phase 2 - 数据持久化
- [ ] 集成 SQLite / TypeORM
- [ ] 历史数据存储
- [ ] 历史数据查询接口
- [ ] 历史页面开发
- [ ] 数据聚合统计

### Phase 3 - 用户认证 ⭐
- [ ] 用户表设计
- [ ] 注册/登录接口
- [ ] JWT 认证中间件
- [ ] 前端登录页面
- [ ] 权限守卫

### Phase 4 - 多服务器监控 ⭐
- [ ] Agent 客户端开发
- [ ] 服务器管理接口
- [ ] Agent 注册/心跳机制
- [ ] 多服务器概览页
- [ ] 服务器详情页

### Phase 5 - 告警系统
- [ ] 阈值配置
- [ ] 浏览器通知
- [ ] 邮件通知（可选）
- [ ] 告警记录存储
- [ ] 告警页面开发

### Phase 6 - 高级功能 ⭐
- [ ] 进程管理模块
- [ ] 网络监控模块
- [ ] 数据统计分析
- [ ] 报告导出（PDF）

### Phase 7 - 完善与部署
- [ ] 主题切换
- [ ] 国际化支持
- [ ] Docker 配置
- [ ] Nginx 配置
- [ ] 项目文档完善

---

## 7. 面试亮点

| 亮点 | 说明 |
|-----|------|
| 全栈开发 | 前后端独立完成 |
| 实时通信 | WebSocket 长连接 + 断线重连 |
| TypeScript | 前后端类型安全 |
| NestJS | 企业级后端架构（模块化、依赖注入） |
| 认证安全 | JWT + Passport + bcrypt |
| 微服务架构 | Agent 模式，中心服务器 + 分布式采集 ⭐ |
| 数据可视化 | ECharts 图表 + 实时更新 |
| 数据持久化 | TypeORM + SQLite |
| 容器化部署 | Docker Compose 一键启动 |
| 进程管理 | 系统级操作能力 ⭐ |

### 面试可能被问到的问题

1. **WebSocket 断线重连怎么实现的？**
2. **为什么选 SQLite 而不是 MySQL？**
3. **Agent 和中心服务器怎么通信的？**
4. **JWT 认证流程是怎样的？Token 过期怎么处理？**
5. **如何保证高并发下的实时数据推送？**
6. **TypeORM 的 migrations 怎么管理？**
7. **前端如何优化大量数据的图表渲染？**

---

## 8. 更新日志

### v2.0 (2026-03-17) ⭐ 大版本更新
- 新增用户认证系统（JWT + Passport）
- 新增多服务器监控（Agent 模式）
- 新增进程管理模块
- 新增网络监控模块
- 新增数据统计与分析
- 完善告警系统（邮件通知、告警级别）
- 更新项目结构设计
- 更新面试亮点

### v1.1 (2026-03-17)
- 新增趋势图组件
- 优化需求文档结构
- 添加开发进度追踪

### v1.0 (2026-03-16)
- 项目初始化
- 完成仪表盘核心功能
